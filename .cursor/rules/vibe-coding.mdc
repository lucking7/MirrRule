---
alwaysApply: true
---

# 通用 vibe coding 规则 v3.0

(architecture-neutral; Python/JS/HTML/… adaptive; strict, quantified, executable)

## 1. Must-follow (non-negotiable)

1. **Single source of truth**：以仓库内“现有代码 + 规范化示例（`code-examples/`）+ 更新式文档”为唯一依据；禁止捏造接口/类型/文件/结果。
2. **Examples-first**：在写入任何文件前，**必须先阅读并对齐**对应 `code-examples/`（同语言/同层级/同场景）。
3. **First tool**：代码理解**必须先**调用 `codebase-retrieval`；命中数 **≥3** 才能进入下一步。
4. **Docs = update-style only**：`overview.md / plan.md / task.md` 与模块 `README / INTERFACE / TREE` **只更新不复制**；若不存在，初始化一次后仅覆盖指定段落；禁止副本文件。
5. **No tests unless TDD**：仅当任务显式指定 TDD 时才编写/修改测试。
6. **Technical only**：除 `overview.md` 可简短语义化外，其余文档**只允许技术语言**（路径/符号/行号/命令/输出）。
7. **Logging discipline**：单“流程/请求”核心日志 ≤1 处；**每文件日志 ≤5**；热点循环/渲染路径禁止冗余日志；必须脱敏；禁止无关注释。
8. **Quantified gates**：以可机检的阈值验收（见 §6）。
9. **Memory**：每个任务必须使用 **remeber** 记录关键点；**每阶段 ≥3 条**（label|fact|impact|next）。Every task should remeber key point.
10. **Atomic + revertable**：提交应原子化；一次 VCS revert 可回滚；禁止无关混合修改。
11. **Fast path 可选**：满足条件方可使用简化流程（见 §2.2）。

---

## 2. Workflow

### 2.1 标准流程（跨文件/跨模块/公共接口/文档结构变更）

A) Intake → B) Scope → C) Audit → D) Plan → E) Implement → F) Verify → G) UpdateDocs → H) Schedule/Repeat

### 2.2 快速流程（全部满足才可用）

- 变更文件 ≤1；变更行数 ≤30；无公共 API 变更；单模块；无资源/构建脚本改动。
- 步骤：`codebase-retrieval` → 精确小改（`str-replace-editor` 或小范围编辑）→ `incrementalFormat + detektQuick/等效` → 更新 `task.md` 与 3 条 remeber（无 Task Space 时写入 PR 描述）。

---

## 3. Step 规范（量化输出）

### 3.1 Intake

- 运行 `codebase-retrieval`（path/regex）；**命中 ≥3**（file:line + symbol）。
- 在 `task.md` 记录：模块/入口文件/示例文件路径。
- `remeber.intake.*` ≥3（范围/接口/风险各 ≥1）。

### 3.2 Scope

- 生成 `.kt/.py/.js/.ts/.go/.rs/.cs/.php/.rb/.sh/.html/.css` 变更清单：**path、class/def、func、行号范围、预估 ± 行数**。
- 硬阈值：函数 ≤**80** 行；文件 ≤**500** 行；新增文件数 ≤ 计划值；跨层依赖变更 **=0**。
- `plan.md` 写入受影响文件表（`path:class#func Lx–Ly ~±n`）。
- `remeber.scope.*` ≥3。

### 3.3 Audit

- `view` 精读签名/调用栈；必要时再次 `codebase-retrieval` 验证 UseCase/Repo/模块接口链路。
- 涉外依赖：解析库 ID 与版本，记录参数/行为差异。
- `plan.md` 写入：接口签名逐行、调用点 `file:line`、错误路径/异常策略。
- 违例必须为 **0**：跨层依赖、缺少错误处理、UI/模板中混入业务逻辑。
- `remeber.audit.*` ≥3。

### 3.4 Plan

- 仅技术项：步骤清单、逐步编辑对象与命令、检查点、回滚触发与命令。
- 验收 KPI（示例）：编译警告 **=0**；Lint 违规 **=0**；日志超限 **=0**；硬编码 UI token **=0**；Δ 行数偏差 **≤20%**；所有 gates **Green**。
- 需要审核时：用 MCP feedback enhanced 提交，记录时间戳/结论。
- `remeber.plan.*` ≥3。

### 3.5 Implement

- 小改优先 `str-replace-editor`；新文件用 `save-file`（单次 **≤300** 行）；删除用 `remove-files`（需在 `plan.md` 登记与回滚）。
- 子步完成即跑 `incrementalFormat → lint/静态检查`；阶段末加 **类型检查/构建/完整质量门**（映射见 §5）。
- 日志位置：仅 `Core/Handler/Middleware/Interceptor` 等允许核心点；**Reducer/纯函数/模板渲染/紧密循环 = 0**。
- `task.md` 记录：commit id、diff(+/−)、命令输出（pass/fail）、关键 symbol 行。
- `remeber.exec.*` ≥3。

### 3.6 Verify

- 必须全绿：格式化、Lint、类型检查（若有）、构建、聚合质量门、CI 质量门。
- 失败处理：回滚上个 commit，修正后重跑；禁止带病合并。
- 零化指标：编译警告=0；日志违规=0；硬编码=0。

### 3.7 UpdateDocs（更新式）

- `TREE.md`：由自动任务重生成；首行 **ISO-8601** 时间戳；固定深度/排除。
- `README/INTERFACE`：仅更新 `Updated:` 与“签名段”；正文原样保留；禁止新副本。
- `overview.md` 可简要语义；`plan.md / task.md` 仅技术字段（键值/列表）。
- `remeber.docs.*` ≥3（范围/计数/异常）。

### 3.8 Schedule/Repeat

- 工作日 **20:00Z**：CI 跑 `docsAll + docsCheck`；如有 diff 自动发 PR。
- 每次周期跑后在 `task.md` 追加 `remeber.schedule.*` ≥3（PR 分支、提交号、模块计数）。

---

## 4. Tool 使用规范（优先级与留痕）

- **codebase-retrieval（必用第一步）**
  - 输入：path/regex；可设深度。输出：file:line、symbol、上下文。
  - 证据：记录前三条命中到 `task.md`；**命中为 0 → 停止流程**。
- **view**
  - 用于精读文件/区间/regex；记录文件、行区间、匹配计数。
- **str-replace-editor**
  - 精确替换；要求旧文本唯一 + 两侧 3–5 行上下文；记录前后计数与 hunk 范围；完成后 `view` 复核。
- **save-file**
  - 单次 ≤**300** 行；禁止创建测试文件；记录路径、行数、校验摘要。
- **remove-files**
  - 需在 `plan.md` 登记与回滚策略；记录删除路径与计数。
- **web-search / doc-resolver（必要时）**
  - 每主题 3 条查询（精确/同义/≤30d）；权威来源 ≥2；记录来源、ISO 日期、3 条事实与置信度。
- **MCP feedback enhanced**
  - 计划审批；记录提交时间、结论、影响范围。
- **remeber**
  - 结构：`label|fact|impact|next`；流程末产出 `remeber.summary` 5 条。

---

## 5. Language/Runtime Gate 映射（按项目选择/替换）

| Stack         | Format                | Lint / Static          | Type Check        | Build/Bundle            | Notes                     |
| ------------- | --------------------- | ---------------------- | ----------------- | ----------------------- | ------------------------- |
| Python        | `ruff format`/`black` | `ruff`                 | `mypy` (TDD 才测) | `pip -q install && run` | 脚本入口需 shebang/权限   |
| Node/TS       | `prettier --write`    | `eslint`               | `tsc --noEmit`    | `npm run build`         | `package.json` 脚本化     |
| Web(HTML/CSS) | —                     | `htmlhint`/`stylelint` | —                 | bundler(如 `vite`)      | Token 化/零硬编码         |
| Go            | `gofmt -s -w`         | `golangci-lint`        | —                 | `go build ./...`        | `go vet` 作为 lint 一部分 |
| Rust          | `cargo fmt --all`     | `cargo clippy`         | —                 | `cargo build`           |                           |
| Java/Kotlin   | `spotlessApply`       | `checkstyle`/`detekt`  | —                 | `gradle build`          |                           |
| C#/.NET       | `dotnet format`       | 分析器/`stylecop`      | —                 | `dotnet build`          |                           |
| PHP           | `php-cs-fixer`        | `phpcs`/`phpstan`      | —                 | `composer build`        |                           |
| Shell         | `shfmt -w`            | `shellcheck`           | —                 | —                       |                           |

> 目标：**格式/静态问题=0**，类型检查（若配置）**通过**，构建 **成功**。

---

## 6. Acceptance Metrics（合并前必须全部满足）

1. **本地+CI 通过率 = 100%**；编译/构建警告 **=0**。
2. `codebase-retrieval` 命中 **≥3**；与实际修改处交叉命中 **≥1**。
3. **日志违规 = 0**（核心点 ≤1；每文件 ≤5；禁敏感信息）。
4. **硬编码 UI/样式 = 0**（如适用 Token/变量体系）。
5. **跨层/越权依赖 = 0**（按项目分层定义）。
6. 文档存在率 **=100%**（overview/plan/task/INTERFACE/TREE）；重复文件 **=0**；时间戳=本次执行。
7. 变更行数偏差 **≤20%**（相对 `plan.md` 估算）；未登记删除 **=0**。
8. `remeber` 条目 **≥ 阶段数 × 3**；`remeber.summary = 5`。

---

## 7. Documentation（更新式约束）

- 目录建议：`<module>/docs/current/{overview.md|plan.md|task.md}`（仅一个滚动目录）。
- `overview.md`：允许简短语义 + KPI/回滚；覆盖更新指定段落。
- `plan.md`：仅保留**最新有效计划**并覆盖写入（路径/行号/命令/检查点/回滚）。
- `task.md`：**追加**按时间的技术日志（commit、diff 统计、命令输出、证据行）；禁止叙事句。
- `INTERFACE.md`：签名逐行列表 + `Updated:` 时间戳覆盖；禁止复制副本。
- `TREE.md`：每次重生成；首行 ISO 时间戳；固定深度/排除。

---

## 8. Examples（可直接照抄）

### 8.1 快速流程 — Python 参数微调

- B1：`codebase-retrieval` input=`src/` regex=`def login\(token: str\)` → matches=3（file:line）
- B2：`str-replace-editor` file=`src/auth/api.py` search=`timeout=3_000` replace=`timeout=5_000`
- B3：`ruff format && ruff && mypy --no-warn-unused-ignores`（如配置）→ OK
- B4：`task.md` 追加：
  - commit=`abc123` diff=`+1/-1` commands=`ruff OK; mypy OK` evidence=`api.py L88`
  - remeber.exec.1 `label=scope|fact=api.py L88 only|impact=no cross-module|next=canary`
  - remeber.exec.2 `label=api|fact=signature unchanged|impact=callers=0|next=none`
  - remeber.exec.3 `label=risk|fact=+2s|impact=P95 shift|next=monitor`

### 8.2 标准流程 — Node/TS 文档刷新

- A1：`codebase-retrieval` hits for `IUserRepo` = 5 → `task.md`
- A2：`plan.md`：affected=`README.md, INTERFACE.md, TREE.md`（无逻辑改动）
- A3：`view` 验证 → `INTERFACE.md` 缺 2 个签名
- A4：步骤=`docTree → auditDocs`；检查点=`Updated:` 行刷新；回滚=`git restore`
- A5：运行文档任务；生成 `TREE.md`；刷新 `Updated:`
- A6：`prettier` + `eslint` + `tsc` + `build` 全绿
- A7：文档存在率=100%；重复=0；时间戳=当前
- `remeber.docs.*` 三条完成

---

## 9. 提交规范（最小）

- 标题：`[Fix|Feat|Refactor|Docs] <scope>: <result>`
- 正文：变更点（文件/行）| 影响范围 | 回滚命令 | 任务文档路径
- 附：关键 diff 计数与命令输出摘要（通过/失败）。

---

## 10. 执行顺序（Checklist）

1. `codebase-retrieval`（≥3 命中） → 阅读 `code-examples/` → 生成 mapping 列表
2. 更新 `overview.md`（目标/KPI/回滚）
3. 覆盖写入 `plan.md`（路径/行/命令/检查点/回滚 + `remeber` ≥3）
4. 按计划实施（结构化最小变更；保存证据）
5. 追加 `task.md`（commit/diff/输出/证据行 + `remeber` ≥3）
6. 跑本地/CI gates（映射 §5）
7. 更新 `README/INTERFACE/TREE`（更新式）
8. 周期任务挂钩（如适用），记录 `remeber.schedule.*`

> 本规则移除了任何项目/框架专属约束（如 MVI），通过**工具优先、示例优先、更新式文档、量化门槛、可回滚提交、跨语言 Gate 映射**实现对 **Python/JS/HTML/…** 的自适应落地。
