---
alwaysApply: true
description: AI助手代码变更说明行为规范
---

# AI 助手代码变更说明规范

## 核心原则

**除非用户明确要求，否则不要创建 markdown 文档来说明代码变更**

## 具体行为要求

### ✅ 应该做的

- 在回复消息中直接说明做了什么代码变更
- 简洁地描述修改的文件和主要变更内容
- 在回复中解释变更的原因和影响
- 如有必要，在回复中提供关键代码片段说明

### ❌ 绝对禁止的行为

- 主动创建任何 `.md` 文档文件
- 禁止的文件名模式：
  - `*BUILD*.md` (如 BUILD-AND-INTEGRATION.md)
  - `*DEPLOYMENT*.md` (如 DEPLOYMENT-GUIDE.md)
  - `*CHANGES*.md` / `CHANGELOG*.md`
  - `*SUMMARY*.md` / `*GUIDE*.md`
  - `*INSTRUCTIONS*.md` / `*TUTORIAL*.md`
- 生成详细的变更日志文件
- 创建代码变更说明文档
- 主动生成项目文档（除非用户明确请求）
- 创建任何形式的"使用说明"、"部署指南"、"集成文档"

## 例外情况

仅在以下情况下才创建文档：

1. 用户明确要求创建文档
2. 用户询问"能否生成一个说明文档"
3. 项目本身需要文档文件作为功能的一部分

## 实施方式

变更说明应该直接包含在 AI 回复中，例如：

- "我修改了 `src/utils.ts` 文件，添加了新的工具函数..."
- "在 `package.json` 中更新了依赖版本..."
- "重构了组件结构，主要变更包括..."

这样既能让用户了解变更内容，又避免了不必要的文档文件创建。

## 违规处理

如果意外创建了禁止的文档文件：

1. **立即删除**：使用 `delete_file` 工具删除违规文件
2. **在回复中说明**：解释删除原因，并将内容整合到回复消息中
3. **不要询问用户**：直接删除，不需要用户确认

## 自我检查清单

每次完成任务后检查：

- ✅ 是否创建了任何 `.md` 文件？→ 如是，立即删除
- ✅ 变更说明是否在回复中？→ 如否，补充说明
- ✅ 用户是否明确要求文档？→ 如否，不创建文档
