---
description: 项目文件结构和目录组织规则
alwaysApply: false
---

# Surge 项目文件结构规则

## 顶层目录结构

### 输入/输出目录

- **Source/**: 原始规则源文件目录，构建过程中下载的原始文件
- **public/**: 最终输出目录，生成的规则文件供用户使用
- **Build/**: 构建系统源代码目录，包含所有构建逻辑

### 配置文件

- [package.json](mdc:package.json): 项目依赖和脚本配置
- [tsconfig.json](mdc:tsconfig.json): TypeScript 编译配置
- [eslint.config.js](mdc:eslint.config.js): ESLint 代码检查配置

### 主要脚本

- [complete-build.ts](mdc:complete-build.ts): 完整构建流程入口
- [README.md](mdc:README.md): 项目文档和使用说明

## Build/目录详细结构

### 核心入口

- [Build/index.ts](mdc:Build/index.ts): 构建系统主入口
- [Build/process-all-rules.ts](mdc:Build/process-all-rules.ts): 批量规则处理脚本

### 核心库文件 (Build/lib/)

```
lib/
├── rule-sources.ts              # 规则源配置（核心配置文件）
├── rule-source-types.ts         # 类型定义
├── rule-source-processor.ts     # 规则源处理器
├── rule-source-config-loader.ts # 配置加载器
├── cross-platform-rule-parser.ts# 跨平台规则解析
├── parse-filter/               # 规则解析模块
├── rules/                      # 规则处理核心
└── writing-strategy/           # 输出格式策略
```

### 规则处理核心 (Build/lib/rules/)

- `base.ts`: 基础规则处理功能
- `domainset.ts`: 域名集合规则
- `ruleset.ts`: 规则集处理
- `ip.ts`: IP 规则处理

### 输出策略 (Build/lib/writing-strategy/)

- `surge.ts`: Surge 格式输出
- `clash.ts`: Clash 格式输出
- `singbox.ts`: sing-box 格式输出
- `surfboard.ts`: Surfboard 格式输出

### 工具模块 (Build/lib/)

- `fetch-*.ts`: 网络请求相关工具
- `normalize-domain.ts`: 域名标准化
- `process-line.ts`: 行处理工具
- `trie.ts`: 字典树数据结构
- `misc.ts`: 其他工具函数

## 输出目录结构 (Source/ & public/)

### 按规则类型组织

```
├── Surge/Rulesets/           # Surge格式规则文件
│   ├── reject/              # 广告拦截规则
│   ├── proxy/               # 代理规则
│   ├── direct/              # 直连规则
│   ├── stream/              # 流媒体规则
│   ├── apple/               # Apple服务规则
│   └── ...                  # 其他分类规则
├── GeoIP/                   # 地理IP数据库
└── domainset/               # 域名集合文件
```

### 文件命名规范

- `.list`: Surge 规则列表文件
- `.conf`: 配置文件格式
- `.mmdb`: MaxMind 数据库文件
- `.txt`: 纯文本规则文件

## 关键配置文件路径

### 规则源配置

- 主配置: [Build/lib/rule-sources.ts](mdc:Build/lib/rule-sources.ts)
- 类型定义: [Build/lib/rule-source-types.ts](mdc:Build/lib/rule-source-types.ts)

### 构建脚本

- 主构建: [Build/index.ts](mdc:Build/index.ts)
- 完整构建: [complete-build.ts](mdc:complete-build.ts)

### 处理器

- 规则处理: [Build/lib/rule-source-processor.ts](mdc:Build/lib/rule-source-processor.ts)
- 配置加载: [Build/lib/rule-source-config-loader.ts](mdc:Build/lib/rule-source-config-loader.ts)

## 文件处理流程

1. **Source/**: 下载原始文件 → 格式转换 → 规则处理
2. **public/**: 最终输出 → 验证 → 生成使用示例

## 注意事项

- 所有 TypeScript 源文件在 Build/目录下
- 输出文件按客户端类型和规则类型分类组织
- GeoIP 文件为二进制格式，其他均为文本文件
- 项目偏好使用 pnpm 作为包管理器
