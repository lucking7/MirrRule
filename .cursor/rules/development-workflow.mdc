---
globs: *.ts,*.js
description: 开发工作流程和最佳实践指南
---

# 开发工作流程规则

## 包管理器使用

### 首选 pnpm

- 项目配置了`"packageManager": "pnpm@10.15.0"`
- 所有依赖管理优先使用 pnpm 命令
- 避免使用 npm 或 yarn，保持依赖锁定文件一致性

### 常用命令

```bash
pnpm install          # 安装依赖
pnpm build            # 执行构建
pnpm build-profile    # 性能分析构建
pnpm lint             # 代码检查
pnpm test             # 运行测试
```

## 构建系统工作流程

### 开发阶段流程

1. **配置修改**: 编辑 [Build/lib/rule-sources.ts](mdc:Build/lib/rule-sources.ts)
2. **单元测试**: 验证配置格式和处理逻辑
3. **增量构建**: 使用 `pnpm build` 测试部分规则
4. **完整构建**: 使用 [complete-build.ts](mdc:complete-build.ts) 执行完整流程

### 测试验证流程

1. **配置验证**: 检查 rule-sources 配置语法
2. **下载测试**: 验证 URL 可访问性和内容格式
3. **转换测试**: 确保规则格式转换正确
4. **输出验证**: 检查生成文件符合各客户端要求

## 代码质量标准

### TypeScript 要求

- 严格类型检查，使用 [tsconfig.json](mdc:tsconfig.json) 配置
- 所有函数必须有明确的返回类型声明
- 使用接口定义复杂对象结构

### ESLint 规范

- 遵循 [eslint.config.js](mdc:eslint.config.js) 配置
- 使用 Sukka 的 ESLint 配置套件
- 代码提交前必须通过 lint 检查

### 错误处理模式

- 使用 span 追踪进行性能监控
- 网络请求必须包含重试机制
- 文件处理失败时提供详细错误信息
- 支持降级和继续处理策略

## 文件处理最佳实践

### 规则源配置

- URL 使用可靠的源（优先 GitHub raw）
- 配置 fallback URLs 作为备选
- 为每个规则组添加清晰的描述
- 合理设置处理选项（去重、排序、格式转换）

### 输出文件管理

- Source/目录存放原始下载文件
- public/目录存放最终用户文件
- 保持目录结构清晰和一致
- 生成相应的使用示例文档

### 性能优化考虑

- 使用并发下载提高效率
- 合理配置缓存机制
- 大文件处理时考虑内存使用
- 使用流式处理避免内存溢出

## Git 工作流程

### 分支管理

- main 分支保持稳定可用
- 功能开发使用 feature 分支
- 重大更改前进行充分测试

### 提交规范

- 使用清晰的提交信息
- 包含变更的功能或修复描述
- 避免提交生成的文件到版本控制

## 调试和故障排除

### 常见问题处理

- **URL 失效**: 检查 fallback URLs，更新源地址
- **格式错误**: 使用 cross-platform-rule-parser 进行转换
- **编码问题**: 确保 UTF-8 编码处理
- **大文件处理**: 调整内存配置或使用流式处理

### 日志分析

- 使用 trace 系统跟踪执行流程
- 关注错误统计和性能指标
- 定期检查下载成功率和转换质量

### 性能监控

- 使用 build-profile 进行性能分析
- 监控内存使用和处理时间
- 优化并发数量和缓存策略
